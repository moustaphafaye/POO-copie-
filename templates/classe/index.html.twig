

{% extends 'base.html.twig' %}

{% block title %}Hello ClasseController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>


    {# <h1>Hello {{ controller_name }}! moustapha ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/home/moustapha/Documents/Symfony1/POO/src/Controller/ClasseController.php'|file_link(0) }}">src/Controller/ClasseController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/home/moustapha/Documents/Symfony1/POO/templates/classe/index.html.twig'|file_link(0) }}">templates/classe/index.html.twig</a></code></li>
    </ul>

    {% for classe in classes %}
        {{classe.filiere}}<br>
    {% endfor %}
    {% for classe in classes %}
        {{classe.libele}}<br>
    {% endfor %}
    {% for classe in classes %}
        {{classe.niveau}}<br>
    {% endfor %} #}
<div id="listerprof" class="container mt-5">
   <div class="centre">
     <h1 class="hh1">Lister Classe</h1>
     </div>  
<table class="table table-hover ">
    <thead>
        <tr> 
            <th>libele</th>
            <th>filiere</th>
            <th>niveau</th>
            <th>Action</th>
            <th>-</th>
            <th>-</th>
        </tr>
    </thead>
    <tbody>
        {% for classe in classes %}
        <tr>
            <td>{{classe.libele}}</td>
            <td>{{classe.filiere}}</td>
            <td>{{classe.niveau}}</td>
            <td> <a href="{{path('edit_classe',{id:classe.id})}}"><button  class='btn btn-outline-warning'>Modifier </button></a></td>
             <td> <a href="{{path('delete_classe',{id:classe.id})}}"><button class='btn btn-outline-danger' >Supprimer</button></a></td>
             <td> <a href=""><button class='btn btn-outline-danger' type="submit">Détail</button></a></td>
           
              
            
        </tr>
             {% endfor %}

    </tbody>
</table>

 <a href="{{ path('add_classe')}}" ><button style="width: 15em;" class='btn btn-outline-warning'>Creer classe  </button></a>

        <div class="d-flex justify-content-center">
        {% do classes.setPageRange(10) %}
        {{knp_pagination_render(classes,'pagination.html.twig')}}
        </div>
</div>

{% endblock %}
