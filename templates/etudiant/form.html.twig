{% extends 'base.html.twig' %}

{% block title %}Hello AnneeScolaireController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div id="listerprof" class="container mt-5">
<div id="prof" class="example-wrapper">
<div class="centre">
     <h1 class="hh1">Inscription</h1>
     </div> 

    
     {{form_start(forme)}}
    {{ form_row(forme.nomComplet,{'attr':{'placeholder':"nom_Complet"}}) }}
    {{ form_row(forme.sexe,{'attr':{'placeholder':"sexe"}})}}
    {{ form_row(forme.login,{'attr':{'placeholder':"login"}})}}
    {{ form_row(forme.password,{'attr':{'placeholder':"passmard"}})}}
    {# {{ form_row(forme.isVerified,{'attr':{'placeholder':"passmard"}})}} #}
    {{ form_row(forme.matricule,{'attr':{'placeholder':"passmard"}})}}
    {{ form_row(forme.adresse,{'attr':{'placeholder':"adresse"}})}}
    
  <button type:"submit" class="btn btn-success">Enregistrer</button>

    {{form_end(forme)}}

</div>
</div>
{% endblock %}
